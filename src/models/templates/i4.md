+++
title = "Model I 4 - Proces-verbaal over geldigheid en nummering kandidatenlijsten"
language = "nl"
header_right = "Model I 4 - Proces-verbaal over geldigheid en nummering kandidatenlijsten"
footer_right = "Pagina {page} van {total}"
+++

{% macro omissions(groups) %}
{ striped = false, widths = "1 2" }
| Aanduiding in de kieskring(en) | omschrijving verzuim |
| --- | --- |
{%- for group in groups %}
| {{ group.designation|cell }} in {{ group.electoral_district|cell }} | {% for description in group.omission_descriptions %}{{ description|cell }}{% if !loop.last %}<br>{% endif %}{% endfor %} |
{%- endfor %}
{% endmacro %}

## Model I 4

# Proces-verbaal over geldigheid en nummering kandidatenlijsten

Met dit formulier doet het centraal stembureau verslag van de zitting waarin is
besloten over:

- de geldigheid en nummering van de kandidatenlijsten;
- het handhaven van de kandidaten op, en de aanduidingen bovenaan, de kandidatenlijsten.

### Verkiezing

Het gaat om de verkiezing van **{{ election_name }}**

Dag van stemming **{{ election_date }}**

### Zitting

Het betreft de openbare zitting van het centraal stembureau in **{{ public_session.location }}**

Datum zitting **{{ public_session.date }}**

Tijdstip zitting **{{ public_session.time }}**

### Geconstateerde verzuimen

{% if found_omissions.is_empty() %}
Bij het onderzoek naar de kandidatenlijsten waren geen herstelbare verzuimen
geconstateerd.
{% else %}
Bij het onderzoek naar de kandidatenlijsten waren de volgende herstelbare
verzuimen geconstateerd:

{% call omissions(found_omissions) %}{% endcall %}
{% endif %}

### Herstelde verzuimen

{% if recovered_omissions.is_empty() %}
Er zijn geen verzuimen hersteld.
{% else %}
De volgende verzuimen zijn hersteld:

{% call omissions(recovered_omissions) %}{% endcall %}
{% endif %}

### Ongeldige lijsten

{% if invalid_lists.is_empty() %}
Het centraal stembureau besluit dat geen lijst ongeldig is verklaard.
{% else %}
Het centraal stembureau besluit dat de volgende lijsten ongeldig zijn verklaard:

{% call omissions(invalid_lists) %}{% endcall %}
{% endif %}

### Geschrapte kandidaten

{% if removed_candidates.is_empty() %}
Het centraal stembureau besluit dat geen kandidaat van een lijst is geschrapt.
{% else %}
Het centraal stembureau besluit dat de volgende kandidaten van een lijst zijn geschrapt:

{ striped = false, widths = "1 1 2" }
| Aanduiding in de kieskring(en) | naam kandidaat | reden |
| --- | --- | --- |
{%- for group in removed_candidates %}
{%- for candidate in group.candidates %}
| {% if loop.first %}{{ group.designation|cell }} in {{ group.electoral_district|cell }}{% endif %} | {{ candidate.name|cell }} | {{ candidate.reason|cell }} |
{%- endfor %}
{%- endfor %}
{% endif %}

### Geschrapte aanduidingen

{% if removed_designations.is_empty() %}
Het centraal stembureau besluit dat geen aanduiding boven een lijst is geschrapt.
{% else %}
Het centraal stembureau besluit dat de volgende aanduidingen boven een lijst zijn geschrapt:

{ striped = false, widths = "1 1 2" }
| Aanduiding in de kieskring(en) | naam eerste kandidaat op de lijst | reden |
| --- | --- | --- |
{%- for removed in removed_designations %}
| {{ removed.designation|cell }} in {{ removed.electoral_district|cell }} | {{ removed.first_candidate_name|cell }} | {{ removed.reason|cell }} |
{%- endfor %}
{% endif %}

### Gecorrigeerde aanduiding

{% if corrected_designations.is_empty() %}
Het centraal stembureau besluit dat geen aanduiding boven een lijst ambtshalve is aangepast.
{% else %}
Het centraal stembureau besluit dat de volgende aanduidingen boven een lijst ambtshalve zijn aangepast:

{ striped = false, widths = "1 1 2" }
| Naam eerste kandidaat in de kieskring(en) | vermelde aanduiding bij inlevering | aangepaste aanduiding |
| --- | --- | --- |
{%- for corrected in corrected_designations %}
| {{ corrected.first_candidate_name|cell }} in {{ corrected.electoral_district|cell }} | {{ corrected.submitted_designation|cell }} | {{ corrected.edited_designation|cell }} |
{%- endfor %}
{% endif %}

### Geldige lijsten

Het centraal stembureau besluit dat de volgende lijsten geldig zijn verklaard:

{% for district in valid_lists %}
#### Kieskring {{ district.electoral_district|line }}

{% for list in district.lists %}
##### {{ loop.index|upper_alpha }}. {{ list.designation|line }}

{ widths = "auto 1 1 1" }
|  | naam kandidaat | voorletters | woonplaats |
| --- | --- | --- | --- |
{%- for candidate in list.candidates %}
| {{ candidate.position }} | {{ candidate.last_name|cell }} | {{ candidate.initials|cell }} | {{ candidate.locality|cell }} |
{%- endfor %}

@pagebreak

{% endfor %}
{% endfor %}

### Nummering van de kandidatenlijsten

#### Nummering op grond van het aantal stemmen behaald bij de laatstgehouden verkiezing

Eerst zijn de kandidatenlijsten genummerd van de politieke groeperingen die een
of meer zetels hebben behaald bij de laatstgehouden verkiezing, in de volgorde
van de bij die verkiezing op de desbetreffende lijsten uitgebrachte aantallen
stemmen. Voor zover nodig is rekening gehouden met samengevoegde aanduidingen.
Bij een gelijk aantal stemmen is er genummerd via loting.

{ widths = "auto 1 auto" }
| nummer | aanduiding politieke groepering | aantal stemmen bij laatste verkiezing |
| --- | --- | ---: |
{%- for entry in numbered_based_on_votes %}
| {{ entry.position|display|cell }} | {{ entry.designation|cell }} | {{ entry.previous_votes }} |
{%- endfor %}

#### Nummering van de overige lijsten

Vervolgens zijn de overige kandidatenlijsten genummerd in de volgorde van het
aantal kieskringen waarvoor de lijst is ingeleverd. Bij een gelijk aantal
kieskringen is er genummerd via loting.

{ widths = "auto 1 auto" }
| nummer | aanduiding politieke groepering of naam eerste kandidaat | aantal kieskringen waarvoor lijst geldt |
| --- | --- | ---: |
{%- for entry in numbered_based_on_districts %}
| {{ entry.position|display|cell }} | {{ entry.designation|cell }} | {{ entry.districts }} |
{%- endfor %}

### Bezwaren van de aanwezige kiezers

{% match objections %}
{% when Some(objections) %}
{% if objections.is_empty() %}
Tijdens de zitting zijn geen bezwaren ingebracht.
{% else %}
Tijdens de zitting zijn de volgende bezwaren ingebracht:

{% for objection in objections %}a. {{ objection }}
{% endfor %}
{% endif %}
{% when None %}
Tijdens de zitting zijn

- [ ] geen bezwaren ingebracht.
- [ ] de volgende bezwaren ingebracht:

{# room for writing during the session #}
@spacer(30em)

{% endmatch %}
{% if let Some(response) = response_objections %}
{{ response }}
{% endif %}

### Ondertekening

{ style = "data", striped = false, flush-first, widths = "5 4 6" }
| Datum | {{ public_session.date|cell }} |  |

{ style = "data", striped = false, flush-first, widths = "5 4 6", row-height = "3.5em" }
| Naam en handtekening voorzitter | {{ public_session.chair|cell }} | ________ |
{%- for member in public_session.members %}
| {% if loop.first %}Naam en handtekening leden{% endif %} | {{ member|cell }} | ________ |
{%- endfor %}
